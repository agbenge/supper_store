import 'package:flutter/material.dart';

class ItemAddToShopScreen extends StatelessWidget {
  const ItemAddToShopScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Item Add To Shop'),
      ),
      body: Center(
        child: Text('Item Add To Shop Screen'),
      ),
    );
  }
}