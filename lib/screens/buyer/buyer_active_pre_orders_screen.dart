import 'package:flutter/material.dart';

class BuyerActivePreOrdersScreen extends StatelessWidget {
  const BuyerActivePreOrdersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Buyer Active Pre Orders'),
      ),
      body: Center(
        child: Text('Buyer Active Pre Orders Screen'),
      ),
    );
  }
}